<!DOCTYPE html>
<html lang="pl">
<head>
  <title>E-wybory</title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
</head>

<body class="prose mx-auto bg-gray-200">
<header class="w-full flex justify-center">
  <h1 class="my-0 py-6 text-6xl">E-wybory</h1>
</header>
<nav class="w-full flex justify-center">
  <ul class="flex list-none mx-0 px-0">
    <li class="mx-2"><%= link_to "Strona główna", root_path %></li>
    <% if user_signed_in? %>
      <li class="mx-2">
        <%= button_to "Wyloguj", destroy_user_session_path, method: :delete %>
      </li>
      <li class="mx-2">
        <%= link_to "Profil", edit_user_registration_path, method: :delete %>
      </li>
    <% else %>
      <li class="mx-2">
        <%= link_to "Zaloguj", new_user_session_path %>
      </li>
      <li class="mx-2">
        <%= link_to "Zarejestruj", new_user_registration_path %>
      </li>
    <% end %>
  </ul>
</nav>
<p class="notice text-center"><%= notice %></p>
<p class="alert text-center"><%= alert %></p>
<main class="text-center">
  <%= yield %>
</main>
</body>
</html>
